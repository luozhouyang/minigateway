{
  "name": "minigateway",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*",
    "tools/*"
  ],
  "type": "module",
  "scripts": {
    "ready": "vp fmt && vp lint && vp run test -r && vp run build -r",
    "dev": "vp run website#dev",
    "e2e:smoke": "bash ./scripts/e2e-smoke.sh",
    "prepare": "vp config",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "changeset publish",
    "release:check": "vp run check -r && vp run test -r && vp run build -r"
  },
  "devDependencies": {
    "@changesets/cli": "^2.30.0",
    "vite-plus": "0.1.13"
  },
  "overrides": {
    "vite": "npm:@voidzero-dev/vite-plus-core@0.1.13",
    "vitest": "npm:@voidzero-dev/vite-plus-test@0.1.13"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "packageManager": "pnpm@10.0.0",
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild",
      "lightningcss"
    ]
  }
}
