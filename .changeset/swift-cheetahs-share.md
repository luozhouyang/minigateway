---
"@token-gateway/core": minor
"@token-gateway/cli": minor
---

Publish the unified CLI delivery model that embeds the React admin UI into the CLI package, serves the web app from `/ui`, and exposes the admin API from `/admin`.
